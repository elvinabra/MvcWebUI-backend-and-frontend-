@model IEnumerable<MvcWebUI.Entity.Category>

@{ 
    var selectedCategory = Url.RequestContext.RouteData.Values["id"];
}

   
        
            <a class="list-group-item" href="/Home/List"><i class="fas fa-angle-double-right"></i>     All Categories</a>

        @foreach (var item in Model)
        {
            if (selectedCategory != null)
            {
                if (item.Id == int.Parse(selectedCategory.ToString()))
                {

                        <a href="/Home/List/@item.Id" class="list-group-item active"><i class="fas fa-angle-double-right"></i>     @item.Name</a>
                  
                }
                else
                {

                        <a href="/Home/List/@item.Id" class="list-group-item"><i class="fas fa-angle-double-right"></i>     @item.Name</a>

                }
            }
            else
            {

                    <a  href="/Home/List/@item.Id" class="list-group-item"><i class="fas fa-angle-double-right"></i>     @item.Name</a>
             
            }


        }


    </ul>
